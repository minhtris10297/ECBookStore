<ul class="main-menu">
    <li>
        <a href="@Url.Action("Index","ListBooks")">SÁCH TIẾNG VIỆT</a>
        <ul class="sub-menu">
            @foreach (var item in ViewBag.TheLoai)
            {
                <li><a href="@Url.Action("Index","ListBooks",new { theLoai=item})">@item</a></li>
            }
        </ul>
    </li>

    <li>
        <a href="@Url.Action("Index","ListBooks",new { id="SachGiamGia"})">GIẢM GIÁ SỐC </a>
    </li>

</ul>
